# Entorno de la aplicación
ENVIRONMENT=production
DOMAIN=api.opunnence.com

# PostgreSQL (Render o local)
# Si dejas este valor de ejemplo, la app usará SQLite local (opunnence.db) para desarrollo.
DATABASE_URL=postgresql://usuario:contraseña@host:5432/opunnence_db

# Email (Resend)
RESEND_API_KEY=tu_api_key_de_resend
CONTACT_NOTIFICATION_EMAIL=contacto@opunnence.com
